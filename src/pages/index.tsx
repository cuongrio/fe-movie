import { Box } from "@chakra-ui/react";
import HeroHome from "@/components/hero-home";

const Home = () => {
  return (
    <Box mb={8} w="full">
      <HeroHome />
    </Box>
  );
};

export default Home;
